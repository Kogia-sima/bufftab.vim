# Test case
Execute (Initialize):
  let buffers = copy(bufftab#get_bufs())
  unlet buffers[winbufnr(0)]
  let vader_bufnums = keys(buffers)
  unlet buffers

Execute (No Name Buffer):
  let buffers = copy(bufftab#get_bufs())
  AssertEqual buffers[winbufnr(0)].label, '[No Name] '
  AssertEqual buffers[winbufnr(0)].pre, ''
